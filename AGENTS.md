# AGENTS.md

完整协作规则见 **[`.ai/AGENTS.md`](.ai/AGENTS.md)**（开发原则、`src/` / `packages/`、技术栈、常用命令、`.ai/` 目录约定）。

当前根应用为 **遗传算法 TSP 可视化**（TypeScript + Canvas + Web Worker），规格见 `.ai/specs/GA_TSP_Spec.md`。

Claude Code / Codex 相关配置已统一放在 **`.ai/claude/`**、**`.ai/codex/`**；仓库根目录的 `.claude`、`.codex` 为指向上述目录的符号链接，便于工具按默认路径加载。
